# This is the main configuration file for the application.
# ~~~~~

# Secret key
# ~~~~~
# The secret key is used to secure cryptographics functions.
# If you deploy your application to several instances be sure to use the same key!
application.secret="id`uTyYVt`JmD]kJ:sFt:=_jopp;S]FB@pYk=rKin>/rvamefJdfUsw5EtMD_vu["

session.secure=true
session.username=o

# The application languages
# ~~~~~
application.langs="en"


# Logger
# ~~~~~
# You can also configure logback (http://logback.qos.ch/), by providing a logger.xml file in the conf directory .

# Root logger:
logger.root=ERROR

# Logger used by the framework:
logger.play=INFO

# Logger provided to your application:
logger.application=DEBUG

mongodb.servers=["localhost:27017"]
mongodb.db=dfid

neo4j.path=${?DFID_DATA_PATH}

credentials.username=${CRED_USERNAME}
credentials.password=${CRED_PASSWORD}
deployment.password=${DEPLOY_PASSWORD}

address.fraud="fraud@dfid.gov.uk"
address.feedback="devtracker-feedback@dfid.gov.uk"

# This configuration should be reset and pulled out of this file
#smtp.host="email-smtp.us-east-1.amazonaws.com"
#smtp.port=25
#smtp.tls=true
#smtp.ssl=true
#smtp.user="AKIAI2LM5JRVZ7KQK64Q"
#smtp.password="AmP2BNjdzJySsF5OkKycIBEASsQp265iU7P+aMkL9aoP"


smtp.host=${?SMTP_HOST}
smtp.port=${?SMTP_PORT}
smtp.tls=${?SMTP_TLS}
smtp.ssl=${?SMTP_SSL}
smtp.user=${?SMTP_USER}
smtp.password=${?SMTP_PASSWORD}
